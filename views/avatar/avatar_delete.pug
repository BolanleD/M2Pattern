extends ../layout

block content

  h1 Delete Avatar: #{avatar.name}
  
  p Do you really want to delete this Avatar?
    
  form(method='POST' action='')
    div.form-group
      input#id.form-control(type='hidden',name='id', required='true', value=avatar._id )

    button.btn.btn-danger(type='submit') Delete